[build]
builder = "nixpacks"

[deploy]
# SERVICE_MODE controls which service this is:
#   curator  → runs Flask curator dashboard (ideal-compassion service)
#   pipeline → runs daily CA pipeline (paperse-pipeline service, cron job)
startCommand = "python3 launch.py"
